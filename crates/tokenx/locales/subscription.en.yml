subscription:
  provider:
    unknown: "unknown"
    cache: "Subscription cache"
  display:
    account: "Account %{id}"
    stale: "%{name} (stale)"
  metric:
    session: "Session"
    weekly: "Weekly"
    weekly_suffix: "%{name}-wk"
    opus: "Opus"
    monthly: "Monthly"
    credits: "Credits"
    on_demand: "On demand"
    web_search: "Web Search"
    hour_window: "%{hours} Hour"
    left: "%{value} left"
    minute_limit: "%{duration}m limit"
    day_limit: "%{duration}d limit"
    second_limit: "%{duration}s limit"
    limit_n: "Limit %{index}"
    model_fallback: "model"
  reset:
    now: "resets now"
    in_minutes: "resets in %{mins}m"
    in_hours_minutes: "resets in %{hours}h %{mins}m"
    in_hours: "resets in %{hours}h"
    at: "resets %{datetime}"
  error:
    fetch_panicked: "provider fetch panicked"
    fetch_timeout: "provider fetch exceeded the %{secs}s overall timeout"
    worker_disconnected: "subscription fetch worker disconnected"
    keychain_macos_only: "Keychain lookup is only available on macOS"
    keychain_lookup_failed: "Keychain lookup failed for service '%{service}'"
    cache_clock_before_epoch: "system clock is before the Unix epoch"
    cache_serialize: "failed to serialize subscription cache"
    cache_persist: "failed to persist subscription cache `%{path}`"
    cache_read: "failed to read subscription cache `%{path}`"
    cache_malformed: "malformed subscription cache `%{path}`"
    cache_unsupported_schema: "subscription cache `%{path}` has unsupported schema `%{schema}`"
    cache_unsupported_version: "subscription cache `%{path}` has unsupported version %{version}"
    cache_future_timestamp: "subscription cache `%{path}` has a future timestamp %{timestamp} (current time %{now})"
    claude_read_credentials: "Failed to read Claude credentials from %{path}"
    claude_parse_credentials: "Failed to parse Claude credentials from %{path}"
    claude_no_home_or_keychain: "No Claude credentials found: the home directory is unavailable and the keychain lookup failed: %{error}"
    claude_no_credentials: "No Claude credentials found in the current provider locations"
    claude_rejected: "Claude credentials were rejected. Run `claude` to refresh the provider-owned authentication."
    claude_request_failed: "Claude usage request failed (HTTP %{status})"
    claude_no_oauth: "No Claude OAuth credentials. Run 'claude' to log in."
    claude_no_access_token: "No Claude access token."
    codex_no_home: "Cannot locate the home directory for Codex credentials."
    codex_read_auth: "Failed to read Codex auth from %{path}"
    codex_parse_auth: "Failed to parse Codex auth from %{path}"
    codex_no_credentials: "No Codex credentials found at %{path}. Run `codex login` to authenticate."
    codex_no_usable_token: "No usable Codex access token found at %{path}. Run `codex login` to authenticate."
    codex_rejected: "Codex credentials were rejected. Run `codex login` to refresh the provider-owned authentication."
    codex_request_failed: "Codex usage request failed (HTTP %{status})"
    codex_auth_page: "Codex usage returned an authentication page. Run `codex login` to refresh the provider-owned authentication."
    codex_no_tokens: "No Codex tokens."
    codex_no_access_token: "No Codex access token."
    grok_no_home: "cannot locate the home directory for ~/.grok/auth.json"
    grok_read_credentials: "failed to read Grok credentials from %{path}"
    grok_parse_credentials: "failed to parse Grok credentials from %{path}"
    grok_auth_not_object: "Grok auth.json must contain an object"
    grok_auth_entries_none: "Grok auth.json must contain exactly one https://auth.x.ai::* entry with a usable key; found none"
    grok_auth_entries_multiple: "Grok auth.json must contain exactly one https://auth.x.ai::* entry with a usable key; found %{count}"
    grok_missing_user_id: "Grok credential is missing the user_id required by the billing service"
    grok_missing_principal_id: "Grok credential is missing the principal_id required for account identity"
    grok_field_empty: "Grok credential field `%{field}` is empty"
    grok_rejected: "Grok credentials were rejected; refresh the provider-owned authentication with Grok tooling"
    grok_billing_error: "Grok billing service error: %{detail}"
    grok_parse_billing: "failed to parse Grok billing data"
    grok_no_data: "Grok billing response contained no subscription or quota data"
    kimi_no_home: "Cannot locate Kimi Coding Plan credential because the home directory is unavailable"
    kimi_no_credential: "No Kimi Coding Plan credential found at %{path}"
    kimi_request_failed: "Kimi usage request failed (HTTP %{status})"
    kimi_auth_rejected: "%{source} authentication rejected (HTTP %{status})"
    kimi_no_access_token: "No Kimi Coding Plan access token."
    kimi_credential_expired: "Kimi Coding Plan credential has expired. Run `kimi` to refresh the provider-owned authentication."
    kimi_env_required: "%{env} is required for %{source}"
    minimax_session_expired: "MiniMax Token Plan (%{site}) session expired; check your API key"
    minimax_request_failed: "MiniMax Token Plan (%{site}) request failed (HTTP %{status})"
    minimax_api_error: "MiniMax Token Plan (%{site}): %{message}"
    minimax_unknown_error: "unknown error"
    minimax_no_usage: "MiniMax Token Plan (%{site}) returned no parseable usage"
    minimax_no_env: "No %{env} set."
    zai_quota_failed: "Z.ai quota request failed (HTTP %{status})"
    zai_subscription_failed: "Z.ai subscription request failed (HTTP %{status})"
    zai_no_api_key: "No Z.ai coding plan API key set. Configure TOKENX_USAGE_ZAI_CODING_PLAN_API_KEY."
