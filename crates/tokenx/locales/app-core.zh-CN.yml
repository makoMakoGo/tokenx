cache:
  failure:
    read: "读取失败"
    decode: "解码失败"
    clock: "时钟异常"
  error:
    read: "无法读取缓存 `%{path}`：%{error}"
    inspect: "无法读取缓存 `%{path}` 的元数据：%{error}"
    not_regular_file: "无法读取缓存 `%{path}`：缓存路径不是普通文件"
    invalid: "缓存 `%{path}` 无效：%{error}"
    clock_check: "缓存时钟检查失败：%{error}"
    future_timestamp: "缓存 `%{path}` 保存于时间戳 %{saved_at_ms}，晚于当前时间戳 %{now}"
    retry_health_without_metadata: "缓存 `%{path}` 存在可重试的输入健康问题，但缺少重试元数据"
    persist: "无法写入缓存 `%{path}`"

main:
  error:
    runtime_init: "错误：异步运行时初始化失败：%{error}"
    prefix_invalid: "错误"
    prefix_operational: "错误"

paths:
  error:
    home_unavailable: "无法确定 Tokenx 产品目录，因为用户主目录不可用"
    relative_product_root: "解析得到的 Tokenx 产品根目录 `%{path}` 必须是绝对路径"
    relative_override: "%{variable} 路径 `%{path}` 必须是绝对路径"

settings:
  error:
    read: "无法读取设置文件 `%{path}`：%{source}"
    parse: "无法解析设置 JSON `%{path}`：%{source}"
    invalid: "`%{path}` 中的设置无效：%{source}"
    create_dir: "无法创建设置目录 `%{path}`"
    auto_refresh_range: "autoRefreshMs %{value} 无效；应在 %{min}..=%{max} 范围内"
    invalid_scanner: "扫描器设置无效：%{source}"
    duplicate_subscription_provider: "subscription.providers 中的订阅提供方 `%{provider}` 重复"

shared:
  client:
    invalid_id: "无效的客户端 ID `%{raw}`；可选值：%{valid_ids}"

theme:
  error:
    invalid_theme: "主题 `%{name}` 无效；可选值：%{valid}"
    unknown_theme: "未知主题 `%{name}`；可选值：%{valid}"
