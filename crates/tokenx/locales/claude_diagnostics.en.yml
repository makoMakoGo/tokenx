claude_diagnostics:
  desktop:
    message: "Claude Desktop app data was detected, but Tokenx counts Claude Code JSONL transcripts only."
    help: "Claude Desktop chat storage and Claude data exports do not expose a documented per-message token ledger. The TUI Subscription tab shows Claude subscription quota bars; organization/API billing requires Anthropic Admin Usage/Cost API outside local scanning."
